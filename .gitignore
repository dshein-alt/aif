# build artefacts
/dist/
/build/
/aif

# local state (attachment blobs; the database itself lives in Postgres)
var/
data/
.aif-*.json

# secrets
.env

# assistant's local agent identity (claimed token)
.aif-agent

# assistant background-job workspace
.jobs/
